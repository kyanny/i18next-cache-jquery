Demo: i18next.js and jQuery
===========================

How to run:

```
$ ruby -run -e httpd . --port 8080
$ open http://localhost:8080/a.html
$ open http://localhost:8080/b.html
```

Open browser's developer tool -> network tab
